# Claude Code Instructions

## Shipping / Git
- **Never push to GitHub or create commits unless the user explicitly says "Ship".**
- When ready to ship, tag with the next semver and create a GitHub release with notes.
- Never add `Co-Authored-By: Claude` to commit messages.
